name: [LEGADO] Build e Deploy Automático do Cronograma para o Portal

on:
  # ⚠️ só permite rodar manualmente, nunca mais roda sozinho
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow legado desativado
        run: |
          echo "Este workflow foi descontinuado."
          echo "Use update-cronograma.yml para atualizar o portal."
